[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "clinical-trials-ai-mcp"
version = "1.0.3"
readme = "README.md"
description = "MCP server for clinical trials ai. Features search trials, check eligibility, get trial details. From MEOK AI Labs."
license = {file = "LICENSE"}
requires-python = ">=3.10"
authors = [{name = "MEOK AI Labs", email = "nicholas@meok.ai"}]
dependencies = ["mcp>=1.0.0"]
keywords = ["mcp", "ai", "meok", "clinical", "trials"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Topic :: Software Development :: Libraries",
]
[project.urls]
Homepage = "https://meok.ai"
Repository = "https://github.com/CSOAI-ORG/clinical-trials-ai-mcp"
[tool.hatch.build.targets.wheel]
packages = ["."]
only-include = ["server.py"]

[project.scripts]
clinical_trials_ai_mcp = "server:main"
